# Table generation

The final table source builder, Word renderer, and validator are in
`../frozen_workspace/work/five_method_final_tables/`. Final CSVs and the
editable workbook are released under `results/source_data/tables/`.

