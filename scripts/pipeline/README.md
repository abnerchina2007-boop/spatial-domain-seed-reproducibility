# Guarded orchestration

The SEDR locked queue, scientific gate, and resume-aware full pipeline are in
`../frozen_workspace/work/sedr_expansion/`. MERFISH queue/resume scripts are in
`../frozen_workspace/work/merfish_expansion/`. They are retained for provenance
and fail-closed execution; reviewers should start with `../run_full_analysis.py`.

