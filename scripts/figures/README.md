# Figure generation

The final renderer is
`../frozen_workspace/work/final_five_method_package/build_final_figures.py`.
Its two upstream source builders are in
`../frozen_workspace/work/sedr_expansion/build_five_method_candidate_main.py`
and `build_five_method_candidate_supp.py`. The exact input/output map is in
`docs/OUTPUTS.md`; released final renderings provide comparison targets.

