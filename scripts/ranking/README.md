# Empirical method rankings

`../frozen_workspace/work/sedr_expansion/analyze_five_method.py` computes exact
five-method rank distributions using `20^5` combinations per dataset. Average
midranks and fractional tied-winner credits are frozen. Cartesian combinations
are not treated as independent experiments.

